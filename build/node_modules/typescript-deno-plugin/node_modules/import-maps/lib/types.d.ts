/// <reference types="node" />
import { URL } from "url";
export declare type ImportObject = Record<string, string | URL | null>;
export declare type ScopeObject = Record<string, ImportObject>;
export interface ImportMaps {
    imports: ImportObject;
    scopes: ScopeObject;
}
